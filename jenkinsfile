pipeline {
    agent any

    stages {
        stage('Version') {
            steps {
                bat 'python3 --version'
            }
        }
        stage('git') {
            steps {
                echo "git"
            }
        }
    }
}
