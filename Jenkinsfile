pipeline {
    agent any

    stages {
        stage('Version') {
            steps {
                bat 'python --version'
            }
        }
        stage('git') {
            steps {
                echo "git"
            }
        }
    }
}
